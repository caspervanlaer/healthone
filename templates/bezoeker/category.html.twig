{% extends 'base.html.twig' %}

{% block title %}Hello BezoekerController!{% endblock %}

{% block body %}

    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="{{ path('homepage')}}">Home</a></li>
            <li class="breadcrumb-item"><a href="{{ path('categories')}}">Categories</a></li>
            <li class="breadcrumb-item active" aria-current="page">{{ category.name }}</li>
        </ol>
    </nav>
    <div class="row gy-3">
        {% for product in category.products %}
            <div class="col-sm-4 col-md-3 col-lg-2 align-items-center">
                <div class="card mb-4">
                    <div class="card-body text-center">
                        <a href="{{path('product',{'id':product.id}) }}">
                            <img alt="no picture" class="product-img img-responsive center-block" src="{{ asset(product.picture) }}"/>
                        </a>
                        <div class="card-title mb-3">{{ product.name }}</div>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>

{% endblock %}